[← 第 1 段階に戻る](./README.md)　9/22

# 講評モード `/practice` 改修

対応：US-05〜US-09／FR-P01〜P04, P08, P10, P12
既存の録音・笑顔・読み上げ・保存はそのまま残す。

## T-141 条件の受け取り

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-141.1 | `app/practice/page.tsx` を Server Component の薄いラッパーにし、`const sp = await searchParams` → `parseConditions(sp)`。`isEmpty` なら `redirect("/")` | `/practice` 直開きでトップへ | [x] |
| T-141.2 | 既存のクライアント部分を `app/practice/PracticeClient.tsx` に切り出し、`conditions` を props で渡す | - | [x] |
| T-141.3 | 画面上部に `describeConditions(conditions)` を表示 | 「IT／エンジニア／新卒／優しい」 | [x] |
| T-141.4 | 「← トップへ戻る」リンク（既存の「一覧へ」は残す） | - | [x] |

## T-142 テーマ：定番＋自由入力

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-142.1 | テーマ `<select>` の選択肢を `TOPICS_BY_CAREER[conditions.career]` ＋ 末尾に「自由入力」 | 新卒／中途で並びが変わる | [x] |
| T-142.2 | 「自由入力」選択時に `<input maxLength={100}>` を表示。`topic` の実値は「自由入力なら入力欄の値、それ以外は選択値」 | - | [x] |
| T-142.3 | 送信ボタンの `disabled` 条件に「テーマが空」を追加 | 自由入力で空のとき押せない | [x] |

## T-143 口調 → 面接官レベル

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-143.1 | 「口調」`<select>` と `tone` state を削除 | 画面から消える | [x] |
| T-143.2 | `/api/coach` の body を `{ topic, answer, conditions }` に変更 | ネットワークタブで確認 | [x] |

## T-144 保存

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-144.1 | `save()` の body を `{ mode: "practice", conditions, topic, answer, smileScore, feedback, memo }` に変更 | 保存後、履歴に条件付きで出る（09 完了後） | [x] |
| T-144.2 | 保存中は「保存する」を `disabled` にし、二重送信を防ぐ | - | [x] |

## 完了条件

- [x] US-06, US-07 の AC が通る
- [x] US-09 の「保存する」の AC が通る（07 完了後に確認）
