[← 第 2 段階に戻る](./README.md)　9/25

# 模擬面接画面（3）進行・中断・総評・自動保存

対応：US-15〜US-19／FR-I09〜I15, I18／NFR-06

## T-226 進行

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-226.1 | `phase === "submitting"` に入ったら：`turns.length < 3` → `fetchQuestion(turns)`（深掘り）、`=== 3` → `fetchSummary()` | 3 往復目で質問が来ない | [ ] |
| T-226.2 | 「戻る」「やり直し」の UI を置かない。ブラウザの戻るで離脱した場合は状態を復元しない（仕様として割り切る） | - | [ ] |

## T-227 面接中断

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-227.1 | 「面接中断」を `phase !== "idle" && phase !== "done"` で表示 | - | [ ] |
| T-227.2 | `confirm("面接を中断しますか？ここまでの内容で総評を出します")` | - | [ ] |
| T-227.3 | `turns.length >= 1` → `fetchSummary()`、`=== 0` → カメラ停止して `router.push("/")` | 0 往復で履歴が増えない | [ ] |
| T-227.4 | 録音中に中断した場合は録音を破棄する | - | [ ] |

## T-228 総評と自動保存

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-228.1 | `fetchSummary()`：`phase = "summarizing"` → `POST /api/interview/summary` → `summary` | - | [ ] |
| T-228.2 | 成功したら即 `POST /api/sessions` に `{ mode: "interview", conditions, turns, summary }` を送る（08 の T-241 が受ける） | 総評表示と同時に履歴が 1 件増える | [ ] |
| T-228.3 | 保存成功で `phase = "done"`、`savedId` を保持。失敗なら `saveError = true` にして「再保存」ボタン | キーを壊して「再保存」で保存できる | [ ] |
| T-228.4 | 総評を `whitespace-pre-wrap` で表示（講評モードと同じカード） | 3 セクションが空行で分かれる | [ ] |
| T-228.5 | 総評表示時にカメラを停止（`FaceMeter` をアンマウント） | ランプが消える | [ ] |

## T-229 総評後の操作

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-229.1 | 既存の読み上げ UI（再生／停止／続き／最初から／音量／速度）を総評に付ける。自動再生はしない | - | [ ] |
| T-229.2 | 「履歴を見る」（`/history/${savedId}` があればそこへ、無ければ `/history`）「トップへ戻る」 | - | [ ] |

## T-230 失敗時の再試行

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-230.1 | `fetchQuestion` `fetchSummary` の失敗で `error` と `retry: () => void` を state に入れ、「もう一度」で同じ関数を再実行 | `turns` が消えていない | [ ] |
| T-230.2 | 失敗中も「面接中断」は押せる | - | [ ] |

## 完了条件

- [ ] US-15〜US-19 の AC が通る（履歴表示分は 09 完了後）
