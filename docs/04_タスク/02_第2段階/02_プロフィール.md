[← 第 2 段階に戻る](./README.md)　9/24

# プロフィール（ES・職務経歴書・免許資格）

対応：US-20, US-20a, US-21／FR-F01〜F09, FR-X10, FR-T04／REQ-38

## T-202 API `GET/PUT /api/profile`

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-202.1 | `app/api/profile/route.ts`：GET は `requireUserId()` → 本人の行を返す（無ければ `{ es: "", history: [], qualifications: "" }`） | - | [ ] |
| T-202.2 | `lib/profile.ts` に `validateProfile(body)`：`es` ≤ 2,000 文字、`qualifications` ≤ 500 文字、`history` は配列 ≤ 30 件、各行 `year` 1950〜今年の整数・`month` 1〜12 の整数・`text` 1〜100 文字。不正なら 400 と理由 | 31 行目で 400 | [ ] |
| T-202.3 | PUT：検証後 `onConflictDoUpdate` で upsert、`updatedAt` 更新 | 2 回保存して 1 行のまま | [ ] |

## T-203 画面 `/profile`

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-203.1 | `app/profile/page.tsx`（Server）：`requireUserId()` → 本人のプロフィールを取得して `ProfileForm` に渡す | - | [ ] |
| T-203.2 | `app/profile/ProfileForm.tsx`（Client）：`useState` で `{ es, history, qualifications }` を管理 | - | [ ] |
| T-203.3 | セクション「ES（エントリーシート）」：`<textarea rows={10} maxLength={2000}>` ＋ 残り文字数。説明「ガクチカ・自己PR・志望動機の骨子など」 | - | [ ] |
| T-203.4 | セクション「職務経歴書（学歴・職歴）」：行ごとに `<input type=number 年>` `<select 月>` `<input 内容 maxLength=100>` `[削除]`。「＋ 行を追加」で末尾に空行を追加（30 行で `disabled`）。説明「履歴書と同じように、古い順に。例：2016 年 4 月 ○○大学 経済学部 入学」 | 行の追加・削除ができる | [ ] |
| T-203.5 | セクション「免許・資格」：`<textarea rows={3} maxLength={500}>` | - | [ ] |
| T-203.6 | **注意書き**：各セクションの直下に `<p class="text-sm">` で「ここに入力した会社名・学校名などの情報は、面接の質問生成のために AI（外部サービス：Groq）へ送信されます。書きたくない情報は省略してください」。文言は `lib/messages.ts` に定数化して 3 か所で共有 | 保存前から見える | [ ] |
| T-203.7 | 「保存する」1 つで PUT。成功「保存しました」、400 は理由を表示、その他はエラー | 開き直すと残る | [ ] |
| T-203.8 | 「← トップへ戻る」 | - | [ ] |

## T-204 サーバー側での読み出しとプロンプト連携

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-204.1 | `lib/profile.ts`：`getProfile(userId)` と `isProfileEmpty(profile)`（3 項目すべて空） | - | [ ] |
| T-204.2 | `formatProfileForPrompt(profile)`：「【ES】…」「【学歴・職歴】YYYY 年 M 月　内容（1 行ずつ）」「【免許・資格】…」を空でない項目だけ結合 | - | [ ] |
| T-204.3 | `buildSystemPrompt(conditions, profileText?)` に渡す。`/api/coach` で呼ぶ | 登録後の講評に ES・経歴が反映される | [ ] |
| T-204.4 | クライアントから `es` `history` を `/api/coach` `/api/interview` に送る経路が無いことを確認 | ネットワークタブ | [ ] |

## 完了条件

- [ ] US-20, US-20a の AC が通る
- [ ] US-21 のうち講評モード分（登録内容を踏まえた講評／未登録でも動く／ブラウザから送っていない）が通る
