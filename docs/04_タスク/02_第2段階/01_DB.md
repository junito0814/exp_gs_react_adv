[← 第 2 段階に戻る](./README.md)　9/24

# DB（turns・profiles）

対応：要件 6 章

## T-201

| ID | やること | 確認 | 状態 |
| --- | --- | --- | --- |
| T-201.1 | `db/schema.ts`：`sessions.turns` を `jsonb("turns").$type<Turn[]>()` で追加。`Turn = { question: string; answer: string; smileScore: number; answerSeconds: number }` を `lib/types.ts` に定義 | - | [ ] |
| T-201.2 | `profiles` テーブル：`userId text PK`、`es text NOT NULL default ''`、`history jsonb NOT NULL default '[]'`（`$type<HistoryRow[]>()`、`HistoryRow = { year: number; month: number; text: string }` を `lib/types.ts` に）、`qualifications text NOT NULL default ''`、`updatedAt timestamp default now() NOT NULL` | - | [ ] |
| T-201.3 | `npx drizzle-kit generate` → `drizzle/0002_*.sql` を確認 → `migrate` | Neon に `profiles` がある | [ ] |

## 完了条件

- [ ] `db.insert(sessions).values({ turns: [...] })` が型エラーなく書ける
